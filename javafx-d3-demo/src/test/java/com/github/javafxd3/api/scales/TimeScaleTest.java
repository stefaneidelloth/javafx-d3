package com.github.javafxd3.api.scales;

import org.junit.Test;

import com.github.javafxd3.api.AbstractTestCase;

@SuppressWarnings("javadoc")
public class TimeScaleTest extends AbstractTestCase {
	
	

	@Override
	@Test
	public void doTest() {
		
		//D3 d3 = new D3(webEngine);
		
		/*
		Object[] domain = d3.time().scale().domain();
		assertEquals(2, domain.length);
		System.out.println(domain[0].asJsDate());
		System.out.println(domain[1].asJsDate());
		Object[] range = d3.time().scale().range(new Integer[]{0, 5}).range();
		
		*/

		// nice and count
	}
}
